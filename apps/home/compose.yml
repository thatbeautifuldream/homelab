services:
  home:
    image: nginx:1.27-alpine
    container_name: homelab-home
    restart: unless-stopped
    ports:
      - "${HOME_PORT:-80}:80"
    volumes:
      - ./public:/usr/share/nginx/html:ro
